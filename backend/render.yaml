services:
  - type: web
    name: wotnot
    runtime: python
    pythonVersion: 3.10
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn wati.main:app --host 0.0.0.0 --port 10000
    rootDir: backend
    # environment variables and other fields as needed
